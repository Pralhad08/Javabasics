public class Mathclass {
    public static  void main(String[] args) {
        //int result = Math.round(1.1F);
        //int result = (int)Math.ceil(1.1F); highest number near to the given number
        //int result = (int)Math.floor(1.1F); smaller number near to the given number
        //int result = Math.max(1, 2); max number given
        //int result = Math.min(1, 2); min number given
        //double result = Math.random(); generates new double number every time we run this code
        //int result = (int)Math.round(Math.random() *100); generates random integer from 0 to 100
        int result = (int) (Math.random() *100); //generates the integer from 0 to 100
        System.out.println(result);
    }
}
